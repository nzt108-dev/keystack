# Env — KeyStack

| Переменная | Default | Назначение |
|-----------|---------|-----------|
| KEYSTACK_HOME | ~/.keystack | папка БД |
| KEYSTACK_PORT | 4319 | порт дашборда |

Секретов/токенов проект не использует. `.env` не нужен.
